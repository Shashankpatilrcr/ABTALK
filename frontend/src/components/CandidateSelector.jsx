// CandidateSelector.jsx
// Renders a searchable grid of candidate cards
// TODO: Implement

export default function CandidateSelector({ candidates = [] }) {
  return (
    <div>
      <h1>Select a Candidate</h1>
      {/* TODO: Render candidate cards */}
    </div>
  );
}
