// ChatWindow.jsx
// Scrollable message list — renders QuestionBubble for each message
// TODO: Implement

export default function ChatWindow({ messages = [], isLoading = false }) {
  return (
    <div>
      {/* TODO: Render messages + typing indicator */}
    </div>
  );
}
